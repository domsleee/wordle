version: 2.1

jobs:
  build:
    docker:
      - image: domslee/gcctbb:x86
    steps:
      - checkout
      - run:
          command: |
            make -j bin/test bin/solve
  test:
    docker:
      - image: domslee/gcctbb:x86
    steps:
      - checkout
      - run:
          command: |
            make -j bin/test
            mkdir -p databases
            unzip -q .circleci/endGamePowersets_12972_12972.zip -d databases/endGameCache/
            ./bin/test

workflows:
  build_and_test:
    jobs:
      - build
      - test