version: 2.1

jobs:
  build:
    docker:
      - image: domslee/gcctbb:x86
    steps:
      - checkout
      - run:
          command: |
            make -j bin/test bin/solve
  test:
    docker:
      - image: domslee/gcctbb:x86
    steps:
      - checkout
      - run:
          command: |
            make -j bin/test
            ./bin/test

workflows:
  build_and_test:
    jobs:
      - build
      - test